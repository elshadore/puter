general {
    colors = true
    color_good = "#98ff8a"
    color_degraded = "#fdfb7b"
    color_bad = "#ff2d39"
    separator = "  "
    interval = 1
}

order += "cpu_usage"
order += "memory"
order += "disk /home/"
order += "cpu_temperature 0"
order += "volume master"
order += "tztime local"

cpu_usage {
    format = "⚡: %usage"
}

memory {
    format = "💽: %used / %total"
    threshold_degraded = "1G"
}

disk "/home/" {
    format = "💾: %used / %total"
    low_threshold = "5"
    threshold_type = "percentage_avail"
}

cpu_temperature 0 {
    format = "🔥: %degrees °C"
}

volume master {
    format = "🔊: %volume"
    format_muted = "🔈: muted"
}

tztime local {
    format = "%A %d-%m-%Y %H:%M:%S"
}